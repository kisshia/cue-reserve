# CUE Reserve Database

This folder contains the SQL export used for the CUE Reserve system.

## Files

- cue_reserve.sql — contains all tables, schema, and sample data.

## How to import the database:

1. Open phpMyAdmin
2. Create a new database named `cue_reserve`
3. Click Import
4. Choose `cue_reserve.sql`
5. Click Go

The backend will automatically connect using its db.php configuration.
